<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Movie</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen">
  <nav class="bg-blue-600 p-4 text-white flex justify-between items-center">
    <span class="font-bold text-xl">Admin Panel</span>
    <a href="/admin/logout" class="bg-white text-blue-600 px-4 py-2 rounded hover:bg-gray-100 font-semibold">Logout</a>
  </nav>
  <main class="max-w-5xl mx-auto py-10">
    <h2>Add Movie</h2>
    <% if (typeof error !== 'undefined') { %><div><%= error %></div><% } %>
    <form method="POST" action="/admin/movies/add">
      <input name="title" placeholder="Title" required><br>
      <input name="description" placeholder="Description" required><br>
      <input name="genre" placeholder="Genre" required><br>
      <input name="duration" placeholder="Duration (min)" type="number" required><br>
      <input name="releaseDate" placeholder="Release Date" type="date" required><br>
      <input name="language" placeholder="Language" required><br>
      <input name="director" placeholder="Director" required><br>
      <button type="submit">Add Movie</button>
    </form>
  </main>
</body>
</html> 