<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Movie</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen">
  <nav class="bg-blue-600 p-4 text-white flex justify-between items-center">
    <span class="font-bold text-xl">Admin Panel</span>
    <a href="/admin/logout" class="bg-white text-blue-600 px-4 py-2 rounded hover:bg-gray-100 font-semibold">Logout</a>
  </nav>
  <main class="max-w-5xl mx-auto py-10">
    <h2>Edit Movie</h2>
    <% if (typeof error !== 'undefined') { %><div><%= error %></div><% } %>
    <form method="POST" action="/admin/movies/<%= movie._id %>/edit">
      <input name="title" value="<%= movie.title %>" required><br>
      <input name="description" value="<%= movie.description %>" required><br>
      <input name="genre" value="<%= movie.genre %>" required><br>
      <input name="duration" value="<%= movie.duration %>" type="number" required><br>
      <input name="releaseDate" value="<%= movie.releaseDate.toISOString().slice(0,10) %>" type="date" required><br>
      <input name="language" value="<%= movie.language %>" required><br>
      <input name="director" value="<%= movie.director %>" required><br>
      <button type="submit">Update Movie</button>
    </form>
  </main>
</body>
</html> 